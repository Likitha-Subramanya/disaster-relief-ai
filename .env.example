# Copy to .env and fill values. Vite will expose variables prefixed with VITE_.
VITE_MAPBOX_TOKEN=
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_BASE_URL=http://localhost:4000
VITE_TWILIO_ACCOUNT_SID=
VITE_TWILIO_AUTH_TOKEN=
VITE_TWILIO_FROM=

# Backend (Node) AI routing
# Set this in your shell or backend environment (not exposed to the browser)
OPENAI_API_KEY=

